{% extends 'base.html.twig' %}

{% block title %}Hello ConversationsController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>



    <div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/Users/thoma/DEV/Projets/publico/src/Controller/ConversationsController.php'|file_link(0) }}">src/Controller/ConversationsController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Users/thoma/DEV/Projets/publico/templates/conversations/conversations.html.twig'|file_link(0) }}">templates/conversations/index.html.twig</a></code></li>
    </ul>
    </div>
    {% set who = null %}
    {% set width = null %}
{% for conversation in conversations %}

{% if (conversation.who.username is defined) %}

    {% set who = {'entity' : 'user',
                  'name': conversation.who.username,
                  'profile_image' : conversation.who.profileImage,
                  'deleted' : false} %}
    {% elseif (conversation.who.nameOfCompany is defined)  %}

        {% set who = {'entity' : 'company',
                      'name': conversation.who.nameOfCompany,
                      'profile_image' : conversation.who.profileImage,
                      'deleted' : false} %}
    {% else %}

        {% set who = {'entity' : conversation.who.entity,
            'name': conversation.who.nameOfEntity,
            'profile_image' : conversation.who.profileImage,
            'deleted' : true }%}

{% endif %}

    {% if (conversation.width.username is defined) %}
        {% set width = {'entity' : 'user',
            'name': conversation.width.username,
            'profile_image' : conversation.width.profileImage,
            'deleted' : false} %}

    {% elseif(conversation.width.nameOfCompany is defined) %}
        {% set width = {'entity' : 'company',
        'name': conversation.width.nameOfCompany,
        'profile_image' : conversation.width.profileImage,
        'deleted' : false} %}

    {% else %}
            {% set width = {'entity' : conversation.width.entity,
                'name': conversation.width.nameOfEntity,
                'profile_image' : conversation.width.profileImage,
                'deleted' : true }%}
    {% endif %}

        {{ conversation.conversation.createdAt|date("d/m/Y H:i") }} <a href="/conversation/{{ conversation.conversation.uuid }}">
    <br><img src="{{  asset(conversation.width.profileImage)}}" width="50" height="50"/>{{ width.name }}
    <br><br>
              <img src="{{  asset(who.profile_image)}}" width="50" height="50" {{ who.deleted ?  'style="opacity: 0.2"'  }}/>
    {{ who.entity == 'user' ?  '(utilisateur' :  '(entreprise'  }}
    {{ who.deleted ?  ' supprimé)' :  ')'  }}

    {{ who.name }}<br><br></a>{% if(conversation.is_read == false) %} non lu {% endif %}



{% endfor %}






























{% endblock %}